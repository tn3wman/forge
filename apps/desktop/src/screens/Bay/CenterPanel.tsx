export function CenterPanel() {
  return (
    <div
      style={{
        padding: '1rem',
        fontFamily: 'system-ui',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
        color: '#52525b',
      }}
    >
      <span style={{ fontSize: '0.9rem' }}>Editor — Phase 1.14+</span>
    </div>
  );
}
